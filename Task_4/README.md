# <p align="center">Вариант 13</p>

## Жадный алгоритм
* **Определение:** это метод решения оптимизационных задач, который основан на локально наилучшем выборе на каждом этапе вычислений с надеждой, что этот выбор приведет к глобальному оптимальному решению. Ключевое свойство такого подхода состоит в том, что решение принимается немедленно и необратимо на каждом шаге.
* **Анализ алгоритма:**
    * Вычисление коэффициента эффективности: Алгоритм сначала вычисляет коэффициент эффективности (profit/resource) для каждого проекта. Это позволяет определить относительную выгоду от вложения ресурса в каждый проект.project["efficiency"] = project["profit"] / project["resource"]
    * Сортировка проектов: Проекты сортируются по этому коэффициенту в порядке убывания. Это значит, что первыми будут рассматриваться наиболее эффективные проекты.sorted_projects = sorted(projects, key=lambda x: x["efficiency"], reverse=True)
    * Отбор проектов: Начиная с самого эффективного. Проект добавляется в список выбранных, если суммарный расход ресурсов ещё не превышает доступный общий ресурс.if current_resource + project["resource"] <= total_resource:    selected_projects.append(project)    current_resource += project["resource"]
    * Итоговая оценка прибыли: Подсчитывается общая прибыль всех выбранных проектов путём суммирования прибылей.total_profit = sum([project["profit"] for project in selected_projects])
* **Временная сложность:** 
* **Объяснение временной сложности:**
    * 
